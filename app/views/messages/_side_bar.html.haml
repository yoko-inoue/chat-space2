.Side
  .Side-Bar
    .top-items
      %b.user-name
        = current_user.name
    
      %ul
        = link_to '#' do
          <i class= "far fa-edit icon-edit"></i>
        = link_to edit_user_path(current_user) do
          <i class="fas fa-cog icon-cog"></i>
  .Group-List
    %a
      .Group-name
        group-name
      .Group-message
        hello
    %a
      .Group-name
        group-name
      .Group-message
        hi